import { createCustomElement, use } from '@ficusjs/core'
import { getEventBus } from '@ficusjs/event-bus/event-bus'
import { getI18n } from '@ficusjs/i18n/i18n'
import { html, renderer } from '@ficusjs/renderers/uhtml'
import { css } from '@ficusjs/renderers/css'
import { getRouter } from '@ficusjs/router'
import { getAppState, getXStateService } from '@ficusjs/state'
export {
  createCustomElement,
  css,
  getAppState,
  getEventBus,
  getI18n,
  getRouter,
  getXStateService,
  html,
  renderer,
  use
}
